#include <bits/stdc++.h>
#define lol long long
using namespace std;
class BIT
{
    vector<lol> a;
    lol n;
    BIT(lol n)
    {
        this->n = n;
        a.assign(n + 1, 0);
    }
    BIT(vector<lol> a)
    { 
    }
};

int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    return 0;
}