#include <stdio.h>
#include <conio.h>
int main()
{
    char str[2];
    lol i =0;
    scanf("%s",str);
    while(str[i]!='\0')
    {
        prlolf("%c",str[i]);
        i++;
    }  getch();
    return 0;
}
