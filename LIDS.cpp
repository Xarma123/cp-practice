#include <bits/stdc++.h>
#define lol long long
using namespace std;
string n;
lol ans = LONG_LONG_MAX;
lol count;
void solve(lol i, bool l, string ans)
{
    if (i == n.size())
    { 
        eval(ans)
    }
}
lol solve(lol _n)
{
    n = to_string(_n);
    solve(0, true, "");
}
int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    return 0;
}