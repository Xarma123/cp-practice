#include<iostream>
#include<conio.h>
#include<cmath>
using namespace std;
int main(){

lol a ;
cin>>a;

    lol b=5*a*a-4,c=5*a*a+4;
    lol d=sqrt(b),e=sqrt(c);
    lol f=d*d , g=e*e;
    if (f==b||g==c)
    {
        cout<<"it is a fibonacci no";
    }else
    {
        cout<<"not a fibonacci no";
    }
    
    
    getch();
    return 0;






}