#include <bits/stdc++.h>
#define lol long long
using namespace std;

int main()
{
    vector<lol> p;
    lol q = 1;

    while (q < 1e18)
    {
        p.push_back(q);
        q = q * 2;
    }

    lol t;
    cin >> t;
    while (t--)
    {
        
    }

    return 0;
}