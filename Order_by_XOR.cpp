#include <bits/stdc++.h>
#define lol long long
using namespace std;
lol sub[6] = {5, 7, 7, 7, 7, 12};
int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    lol s = 0, e = 1e18;
    while (s != e - 1)
    {
        lol m = (s + e) / 2;
    }

    return 0;
}