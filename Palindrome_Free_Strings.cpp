#include <bits/stdc++.h>
#define lol long long
using namespace std;
int main()
{
    lol t;
    cin >> t;
    for (lol k = 1; k <= t; k++)
    {
        string s;
        cin >> s;
        for (lol i = 0; i < s.size(); i++)
        {
            for (lol j = i + 4; j < s.size(); j++)
            { 
                lol l = i,r=j;
                while(l<r)
                {
                    
                }
            }
        }

        if (f)
            cout << "Case #" << k << ": " << p.size() - I.size() << endl;
        else
            cout << "Case #" << k << ": "
                 << "IMPOSSIBLE" << endl;
    }

    return 0;
}