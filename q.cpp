#include <bits/stdc++.h>
#define lol long long
using namespace std;
int main()
{
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    double a;
    memset(&a, -1, sizeof(double));
    double &b = a;
    if (b == b)
        cout << "Y";

    return 0;
}